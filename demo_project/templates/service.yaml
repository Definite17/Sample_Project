apiVersion: v1
kind: Service
metadata:
  name: {{ include "demo_project.fullname" . }}
  labels:
    {{- include "demo_project.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "demo_project.selectorLabels" . | nindent 4 }}
